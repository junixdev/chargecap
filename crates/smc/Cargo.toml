[package]
name = "smc"
version = "0.1.0"
edition = "2021"
description = "SMC (System Management Controller) access for battery charge control"
authors = ["Junix Villacorta"]
license = "MIT"
repository = "https://github.com/junixdev/chargecap"
# Workspace member with a generic crate name; not meant for crates.io.
publish = false

[dependencies]
# Shares `ChargeControlMode` with the daemon and the menu-bar app.
proto = { path = "../proto" }
